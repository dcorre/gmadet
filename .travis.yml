---
# Config file for automatic testing at travis-ci.org

language: minimal

os:
  - linux

services:
  - docker

before_install:
  #- git pull dcorre/gmadet:travis
  - sudo docker build -t gmadet:travis -f Docker/Dockerfile.travis . || exit 1;

script:
  # run tests
  - sudo docker run -v $(pwd):/home/newuser/gmadet/ gmadet:travis -c 'python3.7 setup.py develop --user; pytest tests' || exit 1;
cache:
  directories:
  - $HOME/.cache/pip
  - $HOME/build/.cache/pip

# perform coverage
after_success:
  - codecov
